import './index.css'
// import {useAuth} from '../../contexts/login-context'

const InputElement=(props)=>{
    // const {dispatchAuth}=useAuth()

    return <div>
        {/* <label className="label">Email Address</label>
        <input type="text" placeholder="jasonDeo.gmail.com" className="input" onChange={(e)=>dispatchAuth({type:"email",payload:e.target.value})}/>
        <label className="label">password</label>
        <input type="password" placeholder="password" className="input" onChange={(e)=>dispatchAuth({type:"password",payload:e.target.value})}/> */}
    </div>
}

export { InputElement}